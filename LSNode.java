/*
 * This class develops the nodes that will formulate the 
 * doubly linked lists
 * 
 * It takes no inputs, and holds pointers to both prev and next
 * and holds head location and data; 
 * 
 * All stacks in this program are built using these nodes
 */
public class LSNode {
    char data;
    LSNode next = null;
    LSNode prev = null;
    int head = -1;

}
